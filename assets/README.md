# Assets

Upload your profile photo here as **`profile.jpg`** — the main README displays it from `assets/profile.jpg`.

To upload: on this folder's page in GitHub, click **Add file → Upload files**, drag the photo in (renamed to `profile.jpg`), and commit.
